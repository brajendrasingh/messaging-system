apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kafka-kraft.fullname" . }}-config

data:
  server.properties: |
    #############################
    # Basic Configuration
    #############################

    node.id=1

    process.roles=broker,controller

    #############################
    # Controller Configuration
    #############################

    controller.quorum.voters=1@{{ include "kafka-kraft.fullname" . }}-0.{{ include "kafka-kraft.kafkaService" . }}:9093

    controller.listener.names=CONTROLLER

    #############################
    # Listeners
    #############################

    listeners=PLAINTEXT://:9092,CONTROLLER://:9093

    advertised.listeners=PLAINTEXT://{{ include "kafka-kraft.fullname" . }}-0.{{ include "kafka-kraft.kafkaService" . }}:9092

    listener.security.protocol.map=CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT

    inter.broker.listener.name=PLAINTEXT

    #############################
    # Log Directories
    #############################

    log.dirs=/var/lib/kafka/data

    #############################
    # Topic Defaults
    #############################

    num.partitions=3

    default.replication.factor=1

    offsets.topic.replication.factor=1

    transaction.state.log.replication.factor=1

    transaction.state.log.min.isr=1

    min.insync.replicas=1

    #############################
    # Networking
    #############################

    socket.send.buffer.bytes=102400

    socket.receive.buffer.bytes=102400

    socket.request.max.bytes=104857600

    #############################
    # Log Retention
    #############################

    log.retention.hours=168

    log.segment.bytes=1073741824

    log.retention.check.interval.ms=300000

    #############################
    # Group Coordinator
    #############################

    group.initial.rebalance.delay.ms=0

    #############################
    # Auto Topic Creation
    #############################

    auto.create.topics.enable=true